"""
Определение задач для всех агентов
"""

from crewai import Task
from typing import Optional


# ============== ЗАДАЧИ ГЛАВНОГО КОУЧА ==============

def create_chief_coach_assessment_task(agent, user_profile: dict) -> Task:
    """Задача: Комплексная оценка профиля и целей кандидата"""
    return Task(
        description=f"""Проведите комплексную оценку профиля кандидата:
            Профиль: {user_profile}
            
            Проанализируйте:
            1. Текущее состояние карьеры
            2. Сформулированные цели
            3. Сильные и слабые стороны
            4. Потенциальные направления развития
            5. Основные вызовы и препятствия
            
            Предоставьте общий анализ для разработки стратегии.""",
        agent=agent,
        expected_output="Детальный анализ профиля и рекомендации для стратегии",
    )


def create_career_strategy_task(agent, assessment_data: dict) -> Task:
    """Задача: Разработка персонального плана карьерного развития"""
    return Task(
        description=f"""На основе оценки профиля разработайте персональный план развития:
            Данные оценки: {assessment_data}
            
            Включите:
            1. Краткосрочные цели (3-6 месяцев)
            2. Среднесрочные цели (6-18 месяцев)
            3. Долгосрочные цели (1-3 года)
            4. Ключевые этапы и вехи
            5. Показатели успеха
            6. План контроля и адаптации
            
            Создайте четкую, мотивирующую стратегию.""",
        agent=agent,
        expected_output="Развернутый персональный план развития карьеры",
    )


def create_progress_monitoring_task(agent, plan_data: dict) -> Task:
    """Задача: Мониторинг прогресса и координация агентов"""
    return Task(
        description=f"""Координируйте работу специализированных агентов для реализации плана:
            План: {plan_data}
            
            Определите:
            1. Какие агенты нужны для каждого этапа
            2. Последовательность их работы
            3. Точки интеграции результатов
            4. Критерии успешного завершения
            5. План мониторинга прогресса
            
            Обеспечьте эффективную координацию между всеми специалистами.""",
        agent=agent,
        expected_output="План координации и мониторинга прогресса",
    )


# ============== ЗАДАЧИ ЭКСПЕРТА ПО РЕЗЮМЕ ==============

def create_resume_analysis_task(agent, current_resume: str) -> Task:
    """Задача: Анализ текущего резюме"""
    return Task(
        description=f"""Проведите детальный анализ резюме:
            Текущее резюме:
            {current_resume}
            
            Оцените:
            1. Структуру и форматирование
            2. Содержание и релевантность
            3. Ключевые слова и ATS-оптимизацию
            4. Ясность и лаконичность
            5. Согласованность с должностью
            6. Выявите пробелы и сильные стороны
            
            Предоставьте конкретные рекомендации по улучшению.""",
        agent=agent,
        expected_output="Детальный анализ резюме с рекомендациями",
    )


def create_resume_optimization_task(agent, current_resume: str, job_requirements: str) -> Task:
    """Задача: Оптимизация резюме для конкретной вакансии"""
    return Task(
        description=f"""Оптимизируйте резюме для конкретной вакансии:
            Текущее резюме:
            {current_resume}
            
            Требования вакансии:
            {job_requirements}
            
            Создайте оптимизированный вариант, который:
            1. Соответствует требованиям вакансии
            2. Использует ключевые слова из описания
            3. Выделяет релевантный опыт
            4. Оптимизирован для ATS-систем
            5. Привлекает внимание рекрутера
            
            Предоставьте финальный вариант и объяснение изменений.""",
        agent=agent,
        expected_output="Оптимизированное резюме с объяснением",
    )


def create_resume_generation_task(agent, user_profile: dict, position: str) -> Task:
    """Задача: Создание нового резюме с нуля"""
    return Task(
        description=f"""Создайте профессиональное резюме с нуля:
            Профиль пользователя: {user_profile}
            Целевая позиция: {position}
            
            Разработайте резюме, которое:
            1. Соответствует стандартам резюме
            2. Оптимизировано для целевой позиции
            3. Использует сильные глаголы и метрики
            4. Ясно представляет опыт и навыки
            5. Имеет правильное форматирование
            
            Создайте 2 варианта: краткий и развернутый.""",
        agent=agent,
        expected_output="Полностью подготовленное резюме в двух вариантах",
    )


# ============== ЗАДАЧИ ОХОТНИКА ЗА ВАКАНСИЯМИ ==============

def create_job_search_task(agent, criteria: dict) -> Task:
    """Задача: Поиск подходящих вакансий"""
    return Task(
        description=f"""Найдите подходящие вакансии согласно критериям:
            Критерии поиска: {criteria}
            
            Ищите вакансии, которые:
            1. Соответствуют требуемым навыкам
            2. Находятся в нужном диапазоне зарплат
            3. В желаемой географии/формате
            4. В компаниях с хорошей репутацией
            5. Предлагают рост и развитие
            
            Предоставьте список из 5-10 лучших вариантов с анализом каждой.""",
        agent=agent,
        expected_output="Список подходящих вакансий с анализом",
    )


def create_job_requirements_analysis_task(agent, job_posting: str) -> Task:
    """Задача: Анализ требований вакансии"""
    return Task(
        description=f"""Проведите детальный анализ требований вакансии:
            Описание вакансии:
            {job_posting}
            
            Проанализируйте:
            1. Обязательные требования
            2. Желаемые требования
            3. Скрытые требования
            4. Требуемые навыки (hard & soft)
            5. Уровень опыта
            6. Компетенции для успеха
            7. Потенциальные вызовы
            
            Оцените, насколько профиль кандидата соответствует требованиям.""",
        agent=agent,
        expected_output="Детальный анализ требований вакансии",
    )


def create_company_research_task(agent, company_name: str) -> Task:
    """Задача: Исследование компании"""
    return Task(
        description=f"""Проведите исследование компании:
            Компания: {company_name}
            
            Соберите информацию о:
            1. Истории и миссии компании
            2. Корпоративной культуре
            3. Структуре и команде
            4. Финансовом состоянии
            5. Репутации работодателя
            6. Перспективах развития
            7. Отзывах сотрудников
            
            Предоставьте полный профиль компании.""",
        agent=agent,
        expected_output="Полный анализ компании и рекомендации",
    )


# ============== ЗАДАЧИ ВЕРИФИКАТОРА ==============

def create_content_verification_task(agent, content: str, content_type: str) -> Task:
    """Задача: Проверка качества контента"""
    return Task(
        description=f"""Проведите проверку качества {content_type}:
            Контент:
            {content}
            
            Проверьте:
            1. Грамматику и орфографию
            2. Пунктуацию и стиль
            3. Логику и последовательность
            4. Точность и уместность
            5. Форматирование
            6. Консистентность
            7. Отсутствие ошибок и опечаток
            
            Предоставьте исправленный вариант с комментариями.""",
        agent=agent,
        expected_output="Проверенный контент с замечаниями",
    )


def create_consistency_check_task(agent, documents: dict) -> Task:
    """Задача: Проверка согласованности документов"""
    return Task(
        description=f"""Проверьте согласованность между документами:
            Документы: {documents}
            
            Убедитесь в:
            1. Совпадении информации между документами
            2. Консистентности формулировок
            3. Соответствии фактов
            4. Отсутствии противоречий
            5. Единстве стиля и подхода
            
            Выявите и документируйте любые расхождения.""",
        agent=agent,
        expected_output="Отчет о согласованности с рекомендациями",
    )


# ============== ЗАДАЧИ КОУЧА ПО НАВЫКАМ ==============

def create_skills_gap_analysis_task(agent, current_skills: list, target_role: str) -> Task:
    """Задача: Анализ пробелов в навыках"""
    return Task(
        description=f"""Проведите анализ пробелов в навыках:
            Текущие навыки: {current_skills}
            Целевая роль: {target_role}
            
            Определите:
            1. Требуемые навыки для роли
            2. Навыки, которые уже есть
            3. Критические пробелы
            4. Желаемые навыки для развития
            5. Приоритизацию развития
            6. Оценку трудности приобретения
            
            Предоставьте приоритизированный список развития.""",
        agent=agent,
        expected_output="Анализ пробелов и приоритезированный план развития",
    )


def create_learning_plan_task(agent, skills_gaps: dict, timeline: str) -> Task:
    """Задача: Создание плана обучения"""
    return Task(
        description=f"""Создайте персональный план обучения:
            Пробелы в навыках: {skills_gaps}
            Временной период: {timeline}
            
            Разработайте план, который включает:
            1. Рекомендуемые курсы и ресурсы
            2. Ожидаемое время на обучение
            3. Порядок изучения навыков
            4. Практические проекты для применения
            5. Сертификации для получения
            6. Метрики прогресса
            
            Предоставьте конкретный, выполнимый план.""",
        agent=agent,
        expected_output="Детальный план обучения и развития",
    )


# ============== ЗАДАЧИ ИНТЕРВЬЮЕРА-МЕНТОРА ==============

def create_interview_prep_task(agent, job_description: str, company_info: str) -> Task:
    """Задача: Подготовка к интервью"""
    return Task(
        description=f"""Подготовьте кандидата к интервью:
            Описание должности: {job_description}
            Информация о компании: {company_info}
            
            Разработайте:
            1. Список вероятных вопросов интервьюера
            2. STAR-структурированные ответы
            3. Вопросы для интервьюера
            4. Рекомендации по поведению
            5. Потенциальные подводные камни
            6. Стратегию переговоров о зарплате
            
            Создайте полный набор материалов для подготовки.""",
        agent=agent,
        expected_output="Полный пакет подготовки к интервью",
    )


def create_interview_practice_task(agent, interview_type: str, company: str) -> Task:
    """Задача: Практикование интервью"""
    return Task(
        description=f"""Проведите практическое интервью-тренировку:
            Тип интервью: {interview_type}
            Компания: {company}
            
            Создайте:
            1. Сценарий интервью с типичными вопросами
            2. Рекомендации по ответам
            3. Оценку ответов кандидата
            4. Обратную связь по улучшениям
            5. Рекомендации по невербальному общению
            
            Симулируйте реальное интервью и предоставьте детальную обратную связь.""",
        agent=agent,
        expected_output="Симуляция интервью с обратной связью",
    )


# ============== ЗАДАЧИ АНАЛИТИКА РЫНКА ==============

def create_market_analysis_task(agent, specialization: str, location: str = "Global") -> Task:
    """Задача: Анализ рынка труда"""
    return Task(
        description=f"""Проведите анализ рынка труда:
            Специальность: {specialization}
            География: {location}
            
            Соберите и проанализируйте:
            1. Спрос на специалистов
            2. Уровни зарплат
            3. Требуемые навыки
            4. Тренды развития
            5. Лучшие компании-работодатели
            6. Перспективы роста
            7. Вызовы и возможности
            
            Предоставьте актуальный анализ рынка.""",
        agent=agent,
        expected_output="Детальный анализ рынка труда",
    )


def create_salary_research_task(agent, position: str, location: str, experience: str) -> Task:
    """Задача: Исследование зарплат"""
    return Task(
        description=f"""Исследуйте диапазоны зарплат:
            Должность: {position}
            Локация: {location}
            Опыт: {experience}
            
            Определите:
            1. Средний уровень зарплаты
            2. Диапазон минимума и максимума
            3. Факторы, влияющие на зарплату
            4. Сравнение с другими позициями
            5. Тренды изменения
            6. Компенсационные пакеты
            7. Рекомендации для переговоров
            
            Предоставьте актуальные данные по зарплатам.""",
        agent=agent,
        expected_output="Исследование зарплат и рекомендации по переговорам",
    )


# ============== ЗАДАЧИ СТРАТЕГА ПЕРСОНАЛЬНОГО БРЕНДА ==============

def create_personal_brand_strategy_task(agent, user_profile: dict) -> Task:
    """Задача: Разработка стратегии персонального бренда"""
    return Task(
        description=f"""Разработайте стратегию персонального бренда:
            Профиль: {user_profile}
            
            Создайте:
            1. Brand statement и уникальное предложение
            2. Ключевые сообщения и narratives
            3. Целевую аудиторию
            4. Каналы для взаимодействия
            5. Содержание и стратегию контента
            6. План развития присутствия
            7. Метрики успеха
            
            Предоставьте полную стратегию персонального брендирования.""",
        agent=agent,
        expected_output="Стратегия персонального бренда",
    )


def create_linkedin_optimization_task(agent, current_profile: dict, target_position: str) -> Task:
    """Задача: Оптимизация LinkedIn профиля"""
    return Task(
        description=f"""Оптимизируйте LinkedIn профиль:
            Текущий профиль: {current_profile}
            Целевая позиция: {target_position}
            
            Улучшите:
            1. Headline и summary
            2. Описание опыта и достижений
            3. Ключевые навыки
            4. Рекомендации и одобрения
            5. Фотографию профиля
            6. SEO-оптимизацию
            7. Стратегию взаимодействия
            
            Создайте привлекательный и заметный профиль.""",
        agent=agent,
        expected_output="Оптимизированный LinkedIn профиль и рекомендации",
    )


def create_portfolio_strategy_task(agent, specialization: str, achievements: list) -> Task:
    """Задача: Стратегия портфолио"""
    return Task(
        description=f"""Создайте стратегию портфолио:
            Специальность: {specialization}
            Достижения: {achievements}
            
            Разработайте:
            1. Структуру портфолио
            2. Проекты для включения
            3. Описания и сценарии
            4. Метрики и результаты
            5. Визуальное представление
            6. Способы демонстрации
            7. План обновления
            
            Создайте привлекающее внимание портфолио.""",
        agent=agent,
        expected_output="Стратегия портфолио с рекомендациями по проектам",
    )
